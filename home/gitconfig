[user]
    name = Phong V. Cao
    email = phongvcao@phongvcao.com

[core]
    editor = command vim
    pager = command vim -c 'let g:fzf_mru_disable_at_vimenter = 1 | PAGER' - 
    excludesfile = ~/.gitignore_global

[color]
    ui = true

[merge]
    tool = vim_mergetool
    conflictstyle = diff3

[mergetool "vim_mergetool"]
    cmd = command vim -f -c "MergetoolStart" "$MERGED" "$BASE" "$LOCAL" "$REMOTE"
    trustExitCode = true
    keepBackup = true
